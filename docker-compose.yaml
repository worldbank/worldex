services:
  db:
    image: postgis-h3:0.1
    volumes:
      - ./data/db:/var/lib/postresql/data
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres