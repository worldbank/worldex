services:
  ui:
    build:
      context: ./ui/
      dockerfile: dev.Dockerfile
    command: yarn start
    ports:
      - 5173:5173
    volumes:
      - ./ui:/app
    env_file:
      - ./ui/.env
    depends_on:
      - api
  api:
    # TODO: possibly mount poetry venv
    build:
      context: ./api
    command: poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    ports:
      - 8000:8000
    volumes:
      - ./api:/app
    depends_on:
      - db
  db:
    build: ./postgis-h3
    volumes:
      - ./data/db:/var/lib/postresql/data
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=worldex
      - POSTGRES_PASSWORD=postgres
